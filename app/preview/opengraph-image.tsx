export { alt, contentType, default, size } from "@/lib/og/siteCard";

// Literal, not an expression - Next extracts segment config statically from the AST
export const revalidate = 86400; // 24 hours
